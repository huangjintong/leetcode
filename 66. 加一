class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        result = ''.join([str(i) for i in digits])
        final=int(result)+1
        fianl_result=[]
        for i in list(str(final)):
            fianl_result.append(int(i))
        return fianl_result
